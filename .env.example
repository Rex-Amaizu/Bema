# Public URL only - do not place WordPress credentials here.
NEXT_PUBLIC_WORDPRESS_URL=http://wp-headless-test.local

# Optional server-only key used by exchange-rate.js and /api/exchange-rate.
EXCHANGE_RATE_API_KEY=
